import React, { FC } from 'react';
import Head from 'next/head';

const Home: FC = () => (
  <div>
    <Head>
      <title>Jenkins</title>
    </Head>

    <main>
      <h1>Hello World!!!</h1>
    </main>
  </div>
);

export default Home;
